<%= form_with model: [commentable, Comment.new] do |f| %>
  <div class="form-group">
    <%= f.rich_text_area :body, class: "form-control", placeholder: "Add a comment" %>
  </div>
  <%= f.submit class: "button is-primary" %>
<% end %>
